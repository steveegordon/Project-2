<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
<!-- avatar here -->
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>